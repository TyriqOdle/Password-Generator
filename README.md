This is a password generator i created using python programming language.

The program utalizes the tkinter package to create a simple UI window for the user to input the information required like, password length, platform/website name and the username for the password. When the submit button is clicked, the passwords are saved to a .txt file.